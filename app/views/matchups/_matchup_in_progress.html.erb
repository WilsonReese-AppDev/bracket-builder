<div class="card mb-2">
  <div class="card-body">
    <% if matchup.option_a.present? && matchup.option_b.present? %>
      <%= render "matchups/versus_form", matchup: matchup %>
    <% else %>
      <%= render "matchups/versus_form_disabled", matchup: matchup %>
    <% end %>
  </div>
</div>